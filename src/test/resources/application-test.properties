# Tests must not require a locally running Camunda instance.
camunda.client.enabled=false
management.health.camunda.enabled=false

# No external model call is performed by the Block 1 skeleton tests.
spring.ai.openai.api-key=test-key
